// Example only: merge into your ASP.NET .NET Framework 4.7.2 application's Global.asax.cs.
// Add a reference to the Akeyless.Bootstrap.Net472 project (or its compiled DLL).
// If you use MVC / Web API, keep your existing registrations; only add the bootstrap call.

using System;
using System.Configuration;
using System.Web;
using Akeyless.Bootstrap;

namespace YourWebApplication
{
    public class MvcApplication : HttpApplication
    {
        protected void Application_Start()
        {
            AkeylessFrameworkBootstrapper.EnrichConfigurationAtStartup();

            // After enrichment, use ConfigurationManager as usual (do not log secrets):
            // var apiKey = ConfigurationManager.AppSettings["ApiSharedKey"];
            // var sql = ConfigurationManager.ConnectionStrings["DefaultConnection"].ConnectionString;

            // Or delegate your existing static helper to AppConfiguration once:
            // var apiKey = AppConfiguration.GetAppSetting("ApiSharedKey");

            // ... your existing Application_Start logic (routes, bundles, DI, etc.)
        }
    }
}
